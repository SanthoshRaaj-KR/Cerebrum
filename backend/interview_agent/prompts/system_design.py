PROMPT = """This interview mode is SYSTEM DESIGN, scaled to a fresher \
candidate: pick ONE small, well-scoped system (for example a URL shortener, \
a basic rate limiter, a simple chat app, or a to-do list API) rather than \
anything requiring distributed-systems experience. Walk them through it \
step by step - requirements, a rough API/data model, then one or two \
follow-up twists (e.g. "what if this got 10x traffic overnight?") - rather \
than expecting a full whiteboard architecture unprompted."""
